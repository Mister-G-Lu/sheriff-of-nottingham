# Core dependencies
Pillow>=8.0
pygame>=2.5.0

# Testing
pytest>=7.0
pytest-timeout>=2.1.0
pytest-cov>=4.0.0
pytest-xdist>=3.0.0
pytest-mock>=3.10.0

# Code Quality
ruff>=0.1.0  # Modern fast linter with auto-fix (replaces flake8, isort, pyupgrade)
vulture>=2.7  # Dead code detection (optional - for deep analysis)
pylint>=2.17.0  # Comprehensive code analysis (optional - for deep analysis)

# Building executables (optional - only needed to create .exe/.app files)
# pyinstaller>=5.0  # Uncomment to build standalone executables

# Optional: Enhanced ASCII art display (install for better visuals)
# ascii-magic  # High-quality image to ASCII conversion
# art          # Alternative ASCII art library