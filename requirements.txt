# Core dependencies
Pillow>=8.0
pygame>=2.5.0

# Testing
pytest>=7.0
pytest-timeout>=2.1.0

# Building executables (optional - only needed to create .exe/.app files)
# pyinstaller>=5.0  # Uncomment to build standalone executables

# Optional: Enhanced ASCII art display (install for better visuals)
# ascii-magic  # High-quality image to ASCII conversion
# art          # Alternative ASCII art library